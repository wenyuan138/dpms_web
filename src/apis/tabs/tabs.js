import request from '@/utils/request'

let api = {
  //获取首页tab数据
  getShouYeData: (data) => {
    return request({
      url: '/xxxxxxxxxxxx',
      method: 'get',
      data: {
        body: {
          
        }
      }
    })
  },
}

export default api